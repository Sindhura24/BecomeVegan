<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="becomevegan.com.becomevegan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Restuarant_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:label="Menu" />
        <activity
            android:name=".CustomizeOrder"
            android:label="Customize Your Vegan Order" />
        <activity
            android:name=".VeganFoodMenu"
            android:label="Vegan Restarants" />
        <activity
            android:name=".Chipotle"
            android:label="Menu" />
        <activity android:name=".NonVeganFoodMenu"
            android:label="Non Vegan restarants"/>
        <activity android:name=".nonvegan_chipotle"
            android:label="Menu"/>
    </application>

</manifest>